function newRepChild(rep, tpl_child) { // add a child template at the end of rep
	const rep_children = getRepChildren(rep)
	const max_child = mxaValue(rep_children)
	const new_child = max_child ?
		max_child + 1 : 1
	rep[newRepChild] = tpl_child
}
